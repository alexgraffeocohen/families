<% @slugs.map do |slug| %>
var checkbox = $("input.individual#<%= @resource %>_parse_permission_<%= slug %>")
  if (checkbox.is(':checked')) {
    checkbox.prop("checked", false)
  } else{
  checkbox.prop("checked", true)
  }
<% end %> 