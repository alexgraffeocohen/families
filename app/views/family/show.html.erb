<h1><%= @family.name %></h1>
<div class="jumbotron">
  <p>Family Members:</p>
  <% @family.people.each do |person| %>
    <%= link_to person.first_name.capitalize, person_path(person) %>
  <% end %>
</div>
