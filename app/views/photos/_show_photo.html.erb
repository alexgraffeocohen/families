<div class="photo_div" data-id="<%= photo.id %>">
  <%= link_to image_tag(photo.data_url(:thumb).to_s), photo.data_url(:thumb).to_s, {class: "image_link"}  %>
  <%= link_to "✖", destroy_photo_path(family, album, photo), remote: true, method: :delete, {class: "delete_pic"} %>
  <%= content_tag(:p, photo.caption) %>
</div>