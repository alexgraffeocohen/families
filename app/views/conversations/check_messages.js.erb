<% if @new_messages.length > 0 %>
  <% @new_messages.each do |new_message| %>
   $(".message_all").prepend("<%= j render(:partial => 'messages/show_message', locals: {family: @family, conversation: @conversation, message: new_message}) %>");
  <% end %>
<% end %>
