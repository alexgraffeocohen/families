<% @names.map do |name| %>
  var checkbox = $("#<%= @resource %>_parse_permission_<%= name.downcase %>")
  if (checkbox.is(':checked')) {
    checkbox.prop("checked", false)
  } else{
  checkbox.prop("checked", true)
  }
<% end %> 