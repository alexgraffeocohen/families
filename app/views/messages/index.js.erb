<% if @new_messages.length > 0 %>
  <% @new_messages.each do |new_message| %>
    $(".message_all").append("<%= j render 'show_message', locals: {family: @family, conversation: @conversation} %>")
  <% end %>
<% end %>